# Run artifacts — regenerated by the pipeline, contain absolute paths and job IDs.
# See examples/ for a committed sample of each.
logs/
jobs/
variants/
output/
cache/
wandb/
ratios.csv
metrics.csv
configs/priors_computed.yaml

# Symlinks into project storage (venv, tokenized data, checkpoints).
# These are per-machine and far too large to track.
venv
data
checkpoints

# The examples are deliberately tracked despite the repo-level *.csv rule.
!examples/*.csv
